name: sw2627_flutter_firebase_geargrid
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  flutter_riverpod: ^2.4.9
  cloud_firestore: ^6.8.0
  intl: ^0.20.3
  firebase_storage: ^13.5.0
  image_picker: ^1.2.3
  cloud_functions: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/equipment/